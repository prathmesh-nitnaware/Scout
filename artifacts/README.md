# Artifacts

This folder acts as Scout's local feature store and data persistence layer. 

It contains:
- The compiled candidate features (`candidate_features.parquet`).
- Generated semantic embeddings (`.npy` files) for technical hybrid retrieval.
- Parsed Job Descriptions (`jd_profile_structured.json`).
- Automated pipeline execution reports and evaluations.
